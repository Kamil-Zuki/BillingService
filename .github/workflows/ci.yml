name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build:
    name: Build & Docker
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Restore
        run: dotnet restore BillingService.csproj

      - name: Build
        run: dotnet build BillingService.csproj -c Release --no-restore

      - name: Publish
        run: dotnet publish BillingService.csproj -c Release --no-restore -o ./publish

      - uses: docker/setup-buildx-action@v3

      - name: Docker build
        run: docker build -t billing-service:ci .
